<resources>

    <!-- Base application theme. -->
    <style name="MovieTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Movie_textView" parent="common_Movie_textTextView">
        <item name="android:textSize">@dimen/movie_textSize</item>
        <item name="android:textColor">#727272</item>
    </style>

    <style name="common_Movie_textTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/activity_vertical_margin</item>
        <item name="android:layout_marginTop">@dimen/activity_horizontal_margin</item>
    </style>

</resources>
